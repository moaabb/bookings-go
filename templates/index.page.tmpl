{{template "base" .}}

{{define "content"}}
    <div class="container">
        <div class="row">
            <div class="col">
                <h1>This is the Home page</h1>

                <p>This comes from the template data: {{ index .StringMap "test" }}</p>
            </div>
        </div>
    </div>
{{end}}